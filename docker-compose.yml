services:
  postgres:
    image: postgres:16-alpine
    restart: unless-stopped
    ports:
      - "5432:5432"
    environment:
      POSTGRES_USER: kupi
      POSTGRES_PASSWORD: kupi
      POSTGRES_DB: kupi
    volumes:
      - pgdata:/var/lib/postgresql/data

volumes:
  pgdata:
